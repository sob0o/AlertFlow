apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: alertflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "alertflow"
            - name: POSTGRES_PASSWORD
              value: "alertflow"
            - name: POSTGRES_DB
              value: "alertflow"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: alertflow
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432